name: Update China cloud IP lists

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/update-ip-lists.yml"
      - "scripts/classify_ips.py"
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * *"

permissions:
  contents: write

concurrency:
  group: update-china-cloud-ip-lists
  cancel-in-progress: false

jobs:
  update-ip-lists:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install --disable-pip-version-check -r requirements.txt

      - name: Verify IPinfo token
        env:
          IPINFO_TOKEN: ${{ secrets.IPINFO_TOKEN }}
        run: test -n "$IPINFO_TOKEN"

      - name: Download IP ranges
        run: |
          curl --fail --location --retry 3 --retry-all-errors \
            --output "$RUNNER_TEMP/volcanoenginecn46.txt" \
            https://github.com/Suyunmeng/china-operator-ip/raw/refs/heads/ip-lists/volcanoenginecn46.txt
          curl --fail --location --retry 3 --retry-all-errors \
            --output "$RUNNER_TEMP/ucloudcn46.txt" \
            https://raw.githubusercontent.com/Suyunmeng/china-operator-ip/refs/heads/ip-lists/ucloudcn46.txt

      - name: Generate city IP lists
        env:
          IPINFO_TOKEN: ${{ secrets.IPINFO_TOKEN }}
        run: |
          rm -rf "$RUNNER_TEMP/ip-list"
          python scripts/classify_ips.py \
            --input "$RUNNER_TEMP/volcanoenginecn46.txt" \
            --token "$IPINFO_TOKEN" \
            --output-dir "$RUNNER_TEMP/ip-list/VolcanoEngine-CN"
          python scripts/classify_ips.py \
            --input "$RUNNER_TEMP/ucloudcn46.txt" \
            --token "$IPINFO_TOKEN" \
            --output-dir "$RUNNER_TEMP/ip-list/UCloud-CN"

      - name: Publish changed lists
        env:
          TARGET_BRANCH: ip-list
        run: |
          git fetch origin "$TARGET_BRANCH" || true
          if git show-ref --verify --quiet "refs/remotes/origin/$TARGET_BRANCH"; then
            git switch --create "$TARGET_BRANCH" --track "origin/$TARGET_BRANCH"
          else
            git switch --orphan "$TARGET_BRANCH"
            git rm -rf . >/dev/null 2>&1 || true
          fi

          git rm -rf -- . >/dev/null 2>&1 || true
          cp -R "$RUNNER_TEMP"/ip-list/. .

          git add --all -- .
          if git diff --cached --quiet; then
            echo "No IP-list changes to publish."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "chore: update China cloud city IP lists"
          git push --force origin "$TARGET_BRANCH"
